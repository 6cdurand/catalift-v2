"use client";

// Thin route → the client program page feature component (Programs w3).
import { ClientProgramPage } from "@/features/programs";

export default function ProgramPage() {
  return <ClientProgramPage />;
}
