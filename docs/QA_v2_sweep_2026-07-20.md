# QA v2 sweep — 2026-07-20

**Scope:** READ-ONLY verification of the five features merged in PRs #75–#79. No app logic, schema, auth, or RLS was changed.

**Branch:** `qa/sweep-2026-07-20`

## Base health

| Gate | Command | Result |
|------|---------|--------|
| Type check | `npx tsc --noEmit` | PASS — 0 errors |
| Lint | `npm run lint` | PASS — 0 errors, 54 warnings (pre-existing, no new warnings introduced) |
| Production build | `npm run build` | PASS — 32/32 routes generated |
| Unit tests | `npm run test:unit` | PASS — 59 test files, 479 tests |
| Playwright smoke | `npx playwright test tests/e2e/shell.spec.ts` | PASS — app shell stays mounted across `/feed` soft navigation |

## Target verdicts

| # | Target | Verdict | Evidence | Gap / note |
|---|--------|---------|----------|------------|
| 75 | **Trainer role from signup metadata** — a trainer account has `role='trainer'` and sees the trainer Today toggle. | **PASS** | `supabase/migrations/00011_role_from_signup_metadata.sql` updates `handle_new_user()` to read `raw_user_meta_data->>'mode'/'role'`, validates against `('client','trainer','admin')`, and backfills existing trainer-mode users. `src/features/auth/hooks/use-user-role.ts` queries `public.users.role` (not `user_metadata`) and gates the toggle. `src/features/auth/__tests__/resolve-role.test.ts` (5 tests) locks the G-20 contract that only the DB row can grant trainer access. `TodayPage.test.tsx` renders the Athlete/Trainer toggle only when `role === 'trainer'`. | Live signup → DB → `/today` e2e not run; no Supabase credentials in this environment. |
| 76 | **Trainer Today toggle** — toggle switches to the trainer surface; athlete surface untouched. | **PASS** | `src/app/(app)/today/page.tsx` shows the toggle only for `isTrainerRole` and swaps `TrainerTodaySurface` vs `TodaySurface` based on `isTrainerMode` from `useViewModeStore`. `src/app/(app)/today/__tests__/TodayPage.test.tsx` has two dedicated cases: "renders trainer surface when mode is trainer" and "renders athlete surface when mode is user". `TrainerTodaySurface.test.tsx` (9 tests) covers quick actions, roster stats, client list, and navigation to `/clients` and `/calendar`. | — |
| 77 | **Client file `/clients/[id]`** — tap a client → opens with header + programs + recent history. | **PASS** | `src/app/(app)/clients/page.tsx` pushes `/clients/${client.id}` on row click. `src/app/(app)/clients/[id]/page.tsx` fetches the client via `fetchClients`, programs via `fetchClientProgramsForTrainer`, and history via `fetchWorkoutHistory`, then renders header, Programs, and Recent Workouts. A temporary unit test rendered the page with mocked data and confirmed header (`John Doe`), active program (`Hypertrophy Block 1`), and recent workout (`Push Day`, `12.5k kg`, `24` sets) — see run output below. Empty states also verified. | Navigation click itself not exercised in Playwright; covered by code inspection plus the page-render test. |
| 78 | **Workout builder save** — build one-off, assign to client with no active program, Save → persists and shows on Today/Calendar. | **PASS / PARTIAL** | `src/app/workout/builder/page.tsx` maps `BuilderBlock[]` → `ProgramBlock[]`, creates a single-session `ClientProgram` with `status: 'active'`, and awaits `assignProgramToClient(program)`. On success it calls `upsertClientProgram(result.data)` and returns via `router.back()`. `assignProgramToClient` (`src/features/programs/api/assign.ts`) uses `persist()` with `await supabase.from('client_programs').upsert(...)` and `rowToClientProgram` on read. `w2c1-schedule-save.test.ts` and `client-program-parity.test.ts` simulate the DB write→read round-trip and assert that days, blocks, exercises, scheduleMode, selectedDays, and `sessionPTMap` survive unchanged. `getNextProgramWorkout` then drives the client’s `Up Next` and calendar. | **Known limitation (not a bug to fix here):** if the client already has an active multi-week program, assigning a one-off creates a second `active` row. `useActiveClientProgram` / `useScheduledSessions` both use `programs.find(p => p.status === 'active')`, so whichever row comes first in the returned array wins on Today. The builder page itself documents this in its `COEXISTENCE NOTE`. A live save→reload→Today e2e was not run because no backend credentials were available. |
| 79 | **Coming-soon** — Feed + Community render the polished coming-soon; no crashes. | **PASS** | `src/app/(app)/feed/page.tsx`, `src/app/(app)/community/page.tsx`, and `src/app/(app)/home/page.tsx` all render `<EmptyState variant="coming-soon" />` with matching icon/title/description. `src/components/states/__tests__/states.test.tsx` covers the `coming-soon` badge (`On the roadmap`), title, and description. `npx playwright test tests/e2e/shell.spec.ts` soft-navigated to `/feed` successfully. Production build generated all three routes without errors. | — |

## Evidence snapshots

### `npx tsc --noEmit`
```
$ echo $?
0
```

### `npm run lint`
```
✖ 54 problems (0 errors, 54 warnings)
```

### `npm run build`
```
✓ Compiled successfully in 24.0s
✓ Generating static pages using 7 workers (32/32)
Route (app)
  ...
  ○ /community
  ○ /feed
  ○ /home
  ...
  ○ /today
  ○ /workout/builder
```

### `npm run test:unit`
```
Test Files  59 passed (59)
      Tests  479 passed (479)
```

### Targeted unit-test run for #75–#79 surfaces
```
$ npx vitest run \
  src/app/(app)/today/__tests__ \
  src/features/auth/__tests__/resolve-role.test.ts \
  src/features/programs/__tests__/w2c1-schedule-save.test.ts \
  src/features/programs/client/__tests__/client-program-parity.test.ts \
  src/components/states/__tests__/states.test.tsx

Test Files  9 passed (9)
     Tests  50 passed (50)
```

### Temporary `ClientDetailPage` render test (#77)
A throwaway test was run in `src/app/(app)/clients/[id]/__tests__/page.test.tsx` (removed before commit) with a mocked trainer, client, active program, and workout history.

```
$ npx vitest run src/app/(app)/clients/[id]/__tests__/page.test.tsx

✓ src/app/(app)/clients/[id]/__tests__/page.test.tsx (2 tests) 508ms
  ✓ renders the client file with header, active program, and recent history
  ✓ renders empty states when no active program or history

Test Files  1 passed (1)
     Tests  2 passed (2)
```

### Playwright shell smoke
```
$ NEXT_PUBLIC_SUPABASE_URL=https://igagmdkdzjkxrwnyvgqk.supabase.co \
  NEXT_PUBLIC_SUPABASE_ANON_KEY=test \
  npx playwright test tests/e2e/shell.spec.ts --reporter=line

[1/1] ... App shell › shell stays mounted across tab navigation (soft nav, no reload)
1 passed (33.6s)
```

## Manual verification still needed

1. **Trainer role end-to-end (#75):** complete signup with the "Personal Trainer" switch, confirm `public.users.role = 'trainer'`, then load `/today` and see the Athlete/Trainer toggle.
2. **Workout builder save reload (#78):** with a real Supabase project, build a one-off workout, assign it to a client with **no** active program, save, reload `/today` for that client, and confirm `Up Next` / Calendar show the new session. Then repeat with a client that **already** has an active program and confirm the coexistence ambiguity documented above.
3. **Client file navigation (#77):** from `/clients`, click a client row and confirm soft-nav to `/clients/[id]` renders header, Programs, and Recent Workouts on a real device/browser.

## Reopen / follow-up items

- **#78 active-program coexistence:** decide product behavior when a trainer assigns a one-off to a client that already has an active program (e.g., deactivate existing program, scope one-off differently, or show a warning). The code currently leaves both `active` and `useActiveClientProgram` picks the first returned row.
- **#78 e2e:** add a Playwright test that builds a workout, saves, and reloads to assert persistence on `/today` and `/calendar`.
- **#77 e2e:** add a Playwright test for `/clients` → `/clients/[id]` navigation and content.
